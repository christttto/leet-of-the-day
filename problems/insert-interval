/**
 * @param {number[][]} intervals
 * @param {number[]} newInterval
 * @return {number[][]}
 */
var insert = function(intervals, newInterval) {
    //iterator for checking
        //check if end overlaps
            //check lower start
    for(int i = 0; i< intervals.length; i++){
        var cInterval = intervals[i];
        if(cInterval[1] > newInterval[0]){
            if(cInterval[0] > newInterval[0]){
                //complete overlap, remove the current interval
            }
            else{
                
            }
        }
    }
};